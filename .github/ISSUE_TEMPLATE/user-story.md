## User Story

As a [type of user],
I need [some goal],
So that [some reason].

## Acceptance Criteria

- Given [initial context],
  When [event/action],
  Then [expected result].

- Given [initial context],
  When [event/action],
  Then [expected result].
